import { Component } from '@angular/core';
import { LogService } from './log.service';

@Component({
  selector: 'app-root',
  template: `
    <app-item>
    
    </app-item>
  `
})
export class AppComponent {
  title = 'di';
}
